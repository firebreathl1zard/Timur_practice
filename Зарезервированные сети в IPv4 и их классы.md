Зарезервированные сети в IPv4 и их классы

В сетевых технологиях (протокол IPv4) зарезервированные сети — это специальные диапазоны IP-адресов, 
которые не используются для публичного интернета. Они предназначены для внутренних нужд, тестирования, 
приватных сетей или специальных функций. Эти диапазоны определены в стандартах IETF
(например, RFC 1918 для приватных адресов, RFC 6890 для специальных назначений).

Классы сетей в IPv4
Вот основные классы с их диапазонами, маской подсети и примерами использования. Я выделю зарезервированные диапазоны.

Класс A (первый октет: 1–126, маска по умолчанию: 255.0.0.0 или /8)

Диапазон: 1.0.0.0 — 126.255.255.255
Назначение: Крупные сети (до 16 млн хостов). Подходит для очень больших организаций.
Зарезервированные поддиапазоны:
10.0.0.0/8 (RFC 1918): Приватные сети (используются внутри локальных сетей, не маршрутизируются в интернете). Пример: 10.0.0.0 — 10.255.255.255.
127.0.0.0/8: Loopback (локальный хост). Адрес 127.0.0.1 — стандартный для тестирования (localhost).
0.0.0.0/8: Зарезервировано (0.0.0.0 — текущий хост или "this host").'

Класс B (первый октет: 128–191, маска по умолчанию: 255.255.0.0 или /16)

Диапазон: 128.0.0.0 — 191.255.255.255
Назначение: Средние сети (до 65 тыс. хостов). Для организаций среднего размера.
Зарезервированные поддиапазоны:
172.16.0.0/12 (RFC 1918): Приватные сети (172.16.0.0 — 172.31.255.255).
169.254.0.0/16 (RFC 3927): Link-local адреса (автоматическая настройка IP без DHCP, для временных соединений).

Класс C (первый октет: 192–223, маска по умолчанию: 255.255.255.0 или /24)

Диапазон: 192.0.0.0 — 223.255.255.255
Назначение: Малые сети (до 254 хостов). Для небольших офисов или домашних сетей.
Зарезервированные поддиапазоны:
192.168.0.0/16 (RFC 1918): Приватные сети (192.168.0.0 — 192.168.255.255). Самый распространённый для домашнего использования (например, роутеры по умолчанию используют 192.168.1.0/24).
192.0.0.0/24 и 192.0.2.0/24: Тестовые диапазоны (RFC 5737, для документации и примеров).

Класс D (первый октет: 224–239, маска: не применяется, /4)

Диапазон: 224.0.0.0 — 239.255.255.255
Назначение: Мультикаст (multicast) — для групповой рассылки данных (например, видео-конференции, маршрутизация OSPF).
Зарезервированные: Весь класс зарезервирован для multicast. Не используется для unicast (одиночных хостов). Пример: 224.0.0.1 — все хосты в локальной сети.

Класс E (первый октет: 240–255, маска: не применяется, /4)

Диапазон: 240.0.0.0 — 255.255.255.255
Назначение: Экспериментальные и будущие использования. В основном зарезервировано.
Зарезервированные поддиапазоны:
255.255.255.255: Broadcast-адрес (ограниченный broadcast для локальной сети).
240.0.0.0/4: Полностью зарезервировано (не используется в реальных сетях).
